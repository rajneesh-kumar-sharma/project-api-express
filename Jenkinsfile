pipeline {
  agent any
  stages{
    stage("code"){
      steps{
        git url: "", branch: "master"
      }
    stage("Build"){
      steps{
        sh "npm install && npm Status"
      }
    }
    }
  }
}
